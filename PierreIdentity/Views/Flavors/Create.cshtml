
@model PierreIdentity.Models.Flavor
<h4>Create new flavor</h4>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.EditorFor(model => model.Name)
  <input type="submit" value="Create Flavor" class="btn btn-primary">
}